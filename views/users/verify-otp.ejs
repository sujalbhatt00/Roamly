<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roamly - Email Verification</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-100">

    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="w-full max-w-md p-8 space-y-6 bg-white rounded-xl shadow-2xl">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-red-100 text-red-600">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
                </svg>
            </div>
            <div class="text-center">
                <h2 class="text-3xl font-bold text-gray-900">
                    Verify Your Email
                </h2>
                <p class="mt-2 text-gray-600">
                    Welcome to Roamly! We've sent a 6-digit code to
                    <span class="font-medium text-gray-800"><%= email %></span>.
                </p>
                <% if (error) { %>
                  <div class="mt-2 text-red-600 font-semibold"><%= error %></div>
                <% } %>
            </div>
            <form method="POST" action="/verify-otp" class="space-y-6">
                <input type="hidden" name="email" value="<%= email %>">
                <div>
                    <label for="otp" class="block text-sm font-medium text-gray-700 sr-only">
                        Verification Code
                    </label>
                    <input 
                        type="text" 
                        name="otp" 
                        id="otp" 
                        class="w-full px-4 py-3 text-center text-2xl font-mono tracking-widest border-gray-300 rounded-lg shadow-sm focus:border-red-500 focus:ring-red-500" 
                        placeholder="------"
                        required 
                        maxlength="6"
                        minlength="6"
                        autocomplete="one-time-code"
                      
                        title="Please enter a 6-digit code"
                    >
                </div>
                <div>
                    <button 
                        type="submit" 
                        class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-base font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-all duration-300"
                    >
                        Verify Account
                    </button>
                </div>
            </form>
           <div class="text-center text-sm text-gray-500">
  Didn't get the code? 
  <form method="POST" action="/resend-otp" style="display:inline;">
    <input type="hidden" name="email" value="<%= email %>">
    <button type="submit" class="font-medium text-red-600 hover:text-red-500 hover:underline bg-transparent border-none p-0 m-0">Resend</button>
  </form>
</div>
        </div>
    </div>
</body>
</html>